[project]
name = "planets"
version = "0.2.0"
description = "Newtonian planet gravity simulator (2D, pygame)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "pygame-ce>=2.5",
    "scipy==1.14.0",
]

[tool.uv]
package = false
